# PostgreSQL Database Connection String
# Format: postgresql://username:password@host:port/database
# This is the direct database connection string for server-side use only
# DO NOT use this in client-side code - it contains sensitive credentials
DATABASE_URL=postgresql://username:password@host:port/database

# Supabase Configuration (for client-side use)
# These are already configured in index.html but can be moved here for server-side use
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
